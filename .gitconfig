[user]
    # 必要に応じて.gitconfig.localに追記する
	name = mnishz
	email = mnishz@users.noreply.github.com
[core]
    pager = less -i
    # 必要に応じてutf-8に変換、.gitconfig.localに追記する
    # pager = nkf32 -w | less -i
    # pager = iconv -f cp932 -t utf-8 | less -i
	quotepath = false
[alias]
	graph = log --graph -C -M --pretty=format:\"%C(auto)%h %ad [%an]%d %s\" --date=short --date-order
	windiff = difftool -y -d -t WinMerge
[difftool "WinMerge"]
	path = W:/FreeSoft/WinMergePortable/WinMergePortable.exe
	cmd = \"W:/FreeSoft/WinMergePortable/WinMergePortable.exe\" -f \"*.*\" -e -u -r \"$LOCAL\" \"$REMOTE\"
[include]
    # 最後にlocalの内容で上書き
    path = .gitconfig.local
